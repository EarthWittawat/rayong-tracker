node_modules
.next
out
.env.local
.env.production.local
.env.development.local
.DS_Store
*.tsbuildinfo
next-env.d.ts

# notebook / conda junk
.ipynb_checkpoints/
**/.ipynb_checkpoints/
notebooks/anaconda_projects/
notebooks/external/
notebooks/_cache/
notebooks/cache/
*.ckpt
*.joblib

# pipeline data — large shapefiles + cached rasters + model dumps live under
# <repo>/data. Each teammate copies their own shapefile into data/ following
# data/README.md; nothing in here belongs in git.
data/_cache/
data/_out/
data/landuse_ryg/
data/admin_ryg/

# Supabase CLI scratch
supabase/.temp/

# Claude Code project settings — purely local, don't ship to teammates
.claude/
